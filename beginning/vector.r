# a group of data elements of same type stored in a sequence in R
vec_1 <- c(13, 65.9)
vec_1
typeof(c(12.5))
length(vec_1)
is.character(vec_1) #false
# creating lists
list_1 <- list(12, 'a' = 4, 7.9)
list_1

# atomic vectors
# logical, integer, double, character


# pipe used to express a sequence of multiple operations represented with %>%
